{
  "name": "biteiq",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "mobile"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend & npm run dev --workspace mobile",
    "dev:backend": "npm run dev --workspace backend",
    "dev:mobile": "npm --workspace mobile run dev --",
    "start:backend": "npm run start --workspace backend",
    "check": "npm run check --workspace backend && npm run check --workspace mobile"
  }
}
